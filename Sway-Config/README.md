The programs used in here are 

1 (main) -> SwayFX, waybar, wofi, autotiling

2 (terminal) -> kitty, fish, nvim, tmux, starship

3 (managers) -> imapala, bluetui, swayosd

4 (hypr) -> hypridle, hyprlock

5 (applications) -> nautilus, brave, spotify, libreofice, spotify
